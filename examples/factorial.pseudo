SUBROUTINE mult(x,y)
    t <- 0
    FOR i <- 0 TO x
        t <- t + y
    ENDFOR
    RETURN t
ENDSUBROUTINE

SUBROUTINE fac(x)
    t <- 1
    FOR i <- 1 TO x + 1
        t <- mult(t,i)
    ENDFOR
    RETURN t
ENDSUBROUTINE

SUBROUTINE main()
    fac(6)
ENDSUBROUTINE